
const {
  h,
  render,
  Fragment,
  memo } = App.VDOM;

const {
  useState,
  useEffect,
  useReducer,
  useRef,
  useCallback,
  useMemo } = App.Hooks;

const {
  Link,
  navigateTo,
  addRoute,
  init,
  afterEach } = App.Router;

